{
  "name": "@f/compose-redux-ware",
  "description": "Compose redux middleware.",
  "repository": "git://github.com/micro-js/compose-redux-ware.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/identity": "^1.1.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
